# Supabase Configuration
# Get these from: https://supabase.com → Your Project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key-here
SUPABASE_SERVICE_KEY=your-service-role-secret-key-here

# AI Server Configuration
# For local development (running AI server directly): http://127.0.0.1:8000
# For Docker deployment (docker-compose): http://ai_server:8000
AI_SERVER_URL=http://127.0.0.1:8000

# Server Configuration
PORT=5001
NODE_ENV=development

# Security Configuration
# Generate a secure random string (min 32 characters) for JWT_SECRET
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Rate Limiting
# Window in milliseconds (default: 900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Maximum requests per window (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
# Comma-separated list of allowed origins, or "*" for all
ALLOWED_ORIGINS=*

# Solana Network (optional)
# Options: devnet, testnet, mainnet-beta
SOLANA_NETWORK=devnet

# Logging (optional)
# Options: error, warn, info, debug
LOG_LEVEL=info

# Lottery Scheduler Configuration
# Path to the authority keypair file (same wallet that initialized the lottery)
# Can be: 
#   1. File path: /path/to/keypair.json
#   2. JSON array: [123,45,67,...] (secret key bytes)
#   3. Base58 encoded private key
# If not set, automatic lottery draws are disabled
LOTTERY_AUTHORITY_KEYPAIR=

# Twitter API Configuration (for X profile personalization)
# Get these from: https://developer.twitter.com → Your App → Keys and Tokens
# Required for refreshing user OAuth tokens to fetch bio/tweets
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=
